apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: employee-data
  name: employee-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi